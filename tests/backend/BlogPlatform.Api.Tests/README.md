# BlogPlatform.Api.Tests

These tests exercise the ASP.NET Core API through a real test host and a local
PostgreSQL database.

The suite resets the mutable tables and reapplies the demo seed data so tests
can run repeatedly without manual cleanup.
